name: Build & Deploy Apps + AML (dev)

on:
  workflow_dispatch: {}

permissions:
  id-token: write
  contents: read

env:
  ENV_NAME: dev
  LOCATION: westeurope
  RG_NAME: opal-dev-rg
  IMAGE_TAG: ${{ github.sha }}

jobs:
  build_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Install helper tools (jq + envsubst)
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y jq gettext-base

      - name: Discover infra in Azure (robust)
        run: |
          set -euo pipefail

          echo "=== Discovering resources in RG: ${RG_NAME} ==="

          ACR_NAME=$(az acr list -g "${RG_NAME}" --query "[0].name" -o tsv)
          if [ -z "${ACR_NAME}" ]; then
            echo "ERROR: No Container Registry found in RG ${RG_NAME}. Run Deploy Infra (dev) first."
            exit 1
          fi
          ACR_LOGIN=$(az acr show -n "${ACR_NAME}" -g "${RG_NAME}" --query "loginServer" -o tsv)

          STORAGE=$(az storage account list -g "${RG_NAME}" --query "[0].name" -o tsv)
          if [ -z "${STORAGE}" ]; then
            echo "ERROR: No Storage Account found in RG ${RG_NAME}."
            exit 1
          fi

          SB=$(az servicebus namespace list -g "${RG_NAME}" --query "[0].name" -o tsv)
          if [ -z "${SB}" ]; then
            echo "ERROR: No Service Bus Namespace found in RG ${RG_NAME}."
            exit 1
          fi

          CAE=$(az containerapp env list -g "${RG_NAME}" --query "[0].name" -o tsv)
          if [ -z "${CAE}" ]; then
            echo "ERROR: No Container Apps Environment found in RG ${RG_NAME}."
            exit 1
          fi

          AML=$(az resource list -g "${RG_NAME}" --resource-type "Microsoft.MachineLearningServices/workspaces" --query "[0].name" -o tsv)
          if [ -z "${AML}" ]; then
            echo "ERROR: No Azure ML workspace found in RG ${RG_NAME}."
            exit 1
          fi

          PG_SERVER=$(az resource list -g "${RG_NAME}" --resource-type "Microsoft.DBforPostgreSQL/flexibleServers" --query "[?contains(name,'-pg-ne')].name | [0]" -o tsv)
          if [ -z "${PG_SERVER}" ]; then
            PG_SERVER=$(az resource list -g "${RG_NAME}" --resource-type "Microsoft.DBforPostgreSQL/flexibleServers" --query "[0].name" -o tsv)
          fi
          if [ -z "${PG_SERVER}" ]; then
            echo "ERROR: No Postgres Flexible Server found in RG ${RG_NAME}."
            exit 1
          fi

          echo "ACR_NAME=${ACR_NAME}"
          echo "ACR_LOGIN=${ACR_LOGIN}"
          echo "STORAGE=${STORAGE}"
          echo "SB=${SB}"
          echo "CAE=${CAE}"
          echo "AML=${AML}"
          echo "PG_SERVER=${PG_SERVER}"

          echo "ACR_NAME=${ACR_NAME}" >> $GITHUB_ENV
          echo "ACR_LOGIN=${ACR_LOGIN}" >> $GITHUB_ENV
          echo "STORAGE=${STORAGE}" >> $GITHUB_ENV
          echo "SB=${SB}" >> $GITHUB_ENV
          echo "CAE=${CAE}" >> $GITHUB_ENV
          echo "AML=${AML}" >> $GITHUB_ENV
          echo "PG_SERVER=${PG_SERVER}" >> $GITHUB_ENV

      - name: Login to ACR
        run: |
          set -euo pipefail
          az acr login --name "${ACR_NAME}"

      - name: Set up Docker Buildx
        run: |
          set -euo pipefail
          docker buildx create --use

      - name: Build & push images (linux/amd64) using repo root context
        run: |
          set -euo pipefail

          # IMPORTANT: build context is repo root (.), so Dockerfiles can COPY shared/ etc.
          docker buildx build --platform linux/amd64 \
            -t "${ACR_LOGIN}/opal/web-api:${IMAGE_TAG}" \
            -f src/web_api/Dockerfile . --push

          docker buildx build --platform linux/amd64 \
            -t "${ACR_LOGIN}/opal/orchestrator:${IMAGE_TAG}" \
            -f src/orchestrator/Dockerfile . --push

          docker buildx build --platform linux/amd64 \
            -t "${ACR_LOGIN}/opal/export-worker:${IMAGE_TAG}" \
            -f src/export_worker/Dockerfile . --push

          docker buildx build --platform linux/amd64 \
            -t "${ACR_LOGIN}/opal/billing-service:${IMAGE_TAG}" \
            -f src/billing_service/Dockerfile . --push

          docker buildx build --platform linux/amd64 \
            -t "${ACR_LOGIN}/opal/aml-sd-stub:${IMAGE_TAG}" \
            -f src/aml_sd_stub/Dockerfile . --push

      - name: Create/Update Container Apps
        run: |
          set -euo pipefail

          DB_USER="opaladmin"
          DB_PASS="${{ secrets.POSTGRES_ADMIN_PASSWORD }}"
          DATABASE_URL="postgresql+psycopg://${DB_USER}:${DB_PASS}@${PG_SERVER}.postgres.database.azure.com:5432/opal?sslmode=require"

          COMMON_ENV="ENV_NAME=${ENV_NAME} DATABASE_URL=${DATABASE_URL} STORAGE_ACCOUNT_NAME=${STORAGE} SERVICEBUS_NAMESPACE=${SB}"

          ensure_app () {
            local NAME="$1"
            local IMAGE="$2"
            local INGRESS="$3"
            local PORT="$4"

            if az containerapp show -g "${RG_NAME}" -n "${NAME}" >/dev/null 2>&1; then
              echo "Updating ${NAME}..."
              if [ "${INGRESS}" = "none" ]; then
                az containerapp update -g "${RG_NAME}" -n "${NAME}" \
                  --image "${IMAGE}" \
                  --set-env-vars ${COMMON_ENV}
              else
                az containerapp update -g "${RG_NAME}" -n "${NAME}" \
                  --image "${IMAGE}" \
                  --set-env-vars ${COMMON_ENV} \
                  --ingress "${INGRESS}" --target-port "${PORT}"
              fi
            else
              echo "Creating ${NAME}..."
              if [ "${INGRESS}" = "none" ]; then
                az containerapp create -g "${RG_NAME}" -n "${NAME}" \
                  --environment "${CAE}" \
                  --image "${IMAGE}" \
                  --registry-server "${ACR_LOGIN}" \
                  --min-replicas 1 --max-replicas 3 \
                  --set-env-vars ${COMMON_ENV}
              else
                az containerapp create -g "${RG_NAME}" -n "${NAME}" \
                  --environment "${CAE}" \
                  --image "${IMAGE}" \
                  --registry-server "${ACR_LOGIN}" \
                  --ingress "${INGRESS}" --target-port "${PORT}" \
                  --min-replicas 1 --max-replicas 3 \
                  --set-env-vars ${COMMON_ENV}
              fi
            fi
          }

          ensure_app "opal-web-api-${ENV_NAME}"          "${ACR_LOGIN}/opal/web-api:${IMAGE_TAG}"          "external" 8080
          ensure_app "opal-billing-service-${ENV_NAME}"  "${ACR_LOGIN}/opal/billing-service:${IMAGE_TAG}"  "external" 8080
          ensure_app "opal-orchestrator-${ENV_NAME}"     "${ACR_LOGIN}/opal/orchestrator:${IMAGE_TAG}"     "internal" 8080
          ensure_app "opal-export-worker-${ENV_NAME}"    "${ACR_LOGIN}/opal/export-worker:${IMAGE_TAG}"    "internal" 8080

      - name: Print Web API URL
        run: |
          set -euo pipefail
          FQDN=$(az containerapp show \
            --resource-group "${RG_NAME}" \
            --name "opal-web-api-${ENV_NAME}" \
            --query properties.configuration.ingress.fqdn -o tsv)

          echo "Web API available at: https://${FQDN}"
